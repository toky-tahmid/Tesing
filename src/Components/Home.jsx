// import { useContext } from "react";
// import { AuthContext } from "../Provider/AuthProvider";

const Home = () => {
    // const authInfo = useContext(AuthContext)
    // console.log(authInfo.name)
    return (
        <div>
            <h3 className="3xl">This is Home</h3>
        </div>
    );
};

export default Home;